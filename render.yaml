services:
  - type: web
    name: kpefk-repository
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn config.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        fromValue: "generate your own or use a secret"
      - key: DATABASE_URL
        fromDatabase:
          name: kpefk-db
          property: connectionString

databases:
  - name: kpefk-db
    plan: free
